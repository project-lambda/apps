/**
 * Everything about you, the developer, in one place. Every page, privacy
 * policy and footer reads from here, so change it once.
 */
export const site = {
  /** Shown in the header and as the publisher on every app page. */
  name: 'Abdullah Emroze',
  /** One line under the name on the home page. */
  tagline: 'Small, focused apps that respect your privacy.',
  /** Public contact address. Stores require one on every privacy policy. */
  email: 'emrozeae@gmail.com',
  /** Where you are based. Some privacy laws expect it on the policy. */
  location: 'Dhaka, Bangladesh',
  /** Default accent for pages that aren't about one app. */
  accent: '#1FC98A',
  /**
   * Lines for /app-ads.txt. Only needed once an app shows ads. AdMob gives
   * you the exact line, for example:
   *   'google.com, pub-0000000000000000, DIRECT, f08c47fec0942fa0'
   */
  appAdsTxt: [] as string[],
};
